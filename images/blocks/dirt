function() { //leave the extra parameters empty
  //Draw The Image
  
  //Make the function display the image
  return get(0, 0, 29, 29); //Returns a 27x27 square (25x25 square with room for a strokeweight of 2)
}
